const { Widget } = require('./Widget');
const { TextWidget } = require('./TextWidget');
const { BarWidget } = require('./BarWidget');
const { MenuWidget } = require('./MenuWidget');
const { PanelWidget } = require('./PanelWidget');
const { DialogWidget } = require('./DialogWidget');

module.exports = {
  Widget,
  TextWidget,
  BarWidget,
  MenuWidget,
  PanelWidget,
  DialogWidget
};
